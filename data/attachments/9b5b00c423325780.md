# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: web/register.spec.ts >> Register Feature Tests >> Register should fail with empty email
- Location: tests/web/register.spec.ts:66:9

# Error details

```
TimeoutError: locator.fill: Timeout 10000ms exceeded.
Call log:
  - waiting for locator('[data-qa="signup-name"]')

```

# Page snapshot

```yaml
- generic [ref=e4]: Please wait while your request is being verified...
```

# Test source

```ts
  1  | import { Page } from '@playwright/test';
  2  | 
  3  | export class RegisterPage {
  4  |     constructor(private page: Page) {}
  5  | 
  6  |     // Locators
  7  |     signupNameInput = '[data-qa="signup-name"]';
  8  |     signupEmailInput = '[data-qa="signup-email"]';
  9  |     signupButton = '[data-qa="signup-button"]';
  10 |     existingEmailMessage = 'text=Email Address already exist!';
  11 | 
  12 |     // Methods
  13 |     async gotoRegisterPage() {
  14 |         await this.page.goto('/login');
  15 |     }
  16 | 
  17 |     async enterName(name: string) {
> 18 |         await this.page.locator(this.signupNameInput).fill(name);
     |                                                       ^ TimeoutError: locator.fill: Timeout 10000ms exceeded.
  19 |     }
  20 | 
  21 |     async enterEmail(email: string) {
  22 |         await this.page.locator(this.signupEmailInput).fill(email);
  23 |     }
  24 | 
  25 |     async clickSignupButton() {
  26 |         await this.page.locator(this.signupButton).click();
  27 |     }
  28 | 
  29 |     // Thực hiện chức năng Đăng ký (Bước 1)
  30 |     async signup(name: string, email: string) {
  31 |         await this.enterName(name);
  32 |         await this.enterEmail(email);
  33 |         await this.clickSignupButton();
  34 |     }
  35 | }
```